/// Throwaway probe used during an end-to-end dry run of the quiet-comment
/// action. Deleted before merge.
library;

/// Simplified: scores 1, well under the threshold of 15.
int probe(int x) => x + 1;
