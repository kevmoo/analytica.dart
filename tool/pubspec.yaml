name: _analytica_tool
description: Helper tools for managing and validating skills.
version: 1.0.0
publish_to: none
environment:
  sdk: '^3.12.0'
resolution: workspace

dependencies:
  path: ^1.9.1

dev_dependencies:
  dart_flutter_team_lints: ^3.5.2
  test: ^1.25.0
